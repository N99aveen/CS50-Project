# This file lists all external Python packages required to run the project
# Standard Python libraries (sys, math, datetime) are included by default and don't need to be listed.
pytest>=7.0.0  # Required for running test cases
